import React from "react";
import { NATIVE_COIN_SYMBOL } from "config";

export default function PLS() {
  return (
    <img
      src="/assets/tokens/pls.svg"
      v
      alt={NATIVE_COIN_SYMBOL}
      className={`w-[18px] h-[18px] my-auto`}
    />
  );
}
